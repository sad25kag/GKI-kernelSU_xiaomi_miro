name: Build Miro GKI Vendor
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Build Script
        uses: actions/checkout@v4

      - name: Setup Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential flex bison lld llvm-dev libssl-dev ccache libelf-dev libncurses-dev
          
      - name: Download Toolchain (AOSP Clang)
        run: |
          mkdir clang
          curl -LSs https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/clang-r522830.tar.gz -o clang.tar.gz
          tar -xf clang.tar.gz -C clang
          echo "$(pwd)/clang/bin" >> $GITHUB_PATH

      - name: Clone Kernel Source
        uses: actions/checkout@v4
        with:
          repository: MiCode/Xiaomi_Kernel_OpenSource
          ref: bsp-miro-v-oss
          path: kernel-source
          fetch-depth: 1

      - name: Setup Official KernelSU
        run: |
          cd kernel-source
          # Download KSU Source
          curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -s main
          # Fix Recursive Dependency (Hapus dulu kalau sudah ada, baru tambah satu kali saja)
          sed -i '/source "drivers\/kernelsu\/Kconfig"/d' drivers/Kconfig
          echo 'source "drivers/kernelsu/Kconfig"' >> drivers/Kconfig

      - name: Build Kernel
        run: |
          cd kernel-source
          
          # DEFCONFIG FIX: Berdasarkan log kamu, filenya ada di vendor/sun_defconfig
          # Kita pakai path lengkap agar make tidak bingung
          if [ -f arch/arm64/configs/vendor/sun_defconfig ]; then
              DEFCONFIG="vendor/sun_defconfig"
          elif [ -f arch/arm64/configs/vendor/miro_defconfig ]; then
              DEFCONFIG="vendor/miro_defconfig"
          else
              DEFCONFIG="gki_defconfig"
          fi
          
          echo "---> MENGGUNAKAN DEFCONFIG: $DEFCONFIG"
          
          # Proses Compile (Full LLVM + Skip Warnings yang bikin error)
          make O=out ARCH=arm64 $DEFCONFIG
          make -j$(nproc --all) O=out ARCH=arm64 \
            CC=clang \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-android- \
            LLVM=1 \
            LLVM_IAS=1 \
            KCFLAGS="-w" \
            ALLOW_MISSING_DTB_OVERLAY=1

      - name: Package with AnyKernel3
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3
          rm -rf AnyKernel3/.git
          # Cari file Image secara rekursif biar pasti ketemu
          IMG_PATH=$(find kernel-source/out/arch/arm64/boot -name "Image" | head -n 1)
          if [ -n "$IMG_PATH" ]; then
            cp $IMG_PATH AnyKernel3/
            echo "Kernel Image ditemukan di $IMG_PATH"
          else
            echo "ERROR: Kernel Image tidak ditemukan!"
            exit 1
          fi
          cd AnyKernel3 && zip -r9 "../Kernel-Miro-KSU-Official.zip" .

      - name: Upload Flashable Kernel
        uses: actions/upload-artifact@v4
        with:
          name: Kernel-Miro-KSU-Official
          path: Kernel-Miro-KSU-Official.zip
